<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-800 text-white">
        <div class="form p-10">
            <form action="/create" method="post" >
                <input class="block w-full rounded-lg outLine-none px-5 py-3 bg-zinc-700" type="text" placeholder="enter title here..." name="title">
                <textarea class="block w-full rounded-lg outLine-none px-5 py-3 bg-zinc-700 mt-5" id="" placeholder="enter your notes" name="details" ></textarea>
                <input class="mt-5 bg-blue-600 px-5 py-2 rounded" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks flex gap-5 flex-wrap p-10">

            <% if(files.length > 0 ){%>
                <% files.forEach((val)=>{ %>

                <div class="task w-100 px-5 py-7 rounded-md bg-zinc-700">
                <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
                <div class="flex w-full justify-between items-center mt-3">
                    <a href="/file/<%=val %>" class="text-blue-500 inline-block ">Read More</a>
                    <a href="/edit/<%=val%>" class="text-zinc-400">edit filename</a>

                </div>
                </div>

                <%  })%>
            <%}else{ %>

<h3 class="text-zinc-400">no task yet.</h3>
                <%}%>
           

            

            
        </div>
    </div>
</body>
</html>